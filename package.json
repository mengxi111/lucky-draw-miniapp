{
  "name": "lucky-draw-miniapp",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "description": "A native WeChat mini program demo for free promotional prize draws.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-project.js",
    "assets": "node scripts/generate-assets.js"
  }
}
